global:
  scrape_interval: 5s

scrape_configs:
  - job_name: "ingestor"
    static_configs:
      - targets: ["ingestor:9101"]

  - job_name: "processor"
    static_configs:
      - targets: ["processor:9102"]

rule_files:
  - /etc/prometheus/alerts/alerts.yml

